设计模式

# factory

```
function createBook(name, url){
    let book = {}
    book.name = name
    book.url = url
    return book
}

let feNotes = createBook('fe-notes', 'http://github.com/thonatos/fe-notes')
let beNotes = createBook('be-notes', 'http://github.com/thonatos/be-notes')
```



